<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme: Material 3, no action bar (Compose owns the top bar). -->
    <style name="Theme.SpecTech" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="29">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    </style>

    <!--
        Splash screen theme; falls back to Theme.SpecTech once Compose is up.
        The animated icon is the iOS app icon (ic_launcher) so the brief
        system splash matches the centered logo card the iOS LaunchScreen
        storyboard renders. The richer full-bleed launch artwork is rendered
        afterwards by the Compose `LaunchSplashView` while the session is
        being restored.
    -->
    <style name="Theme.SpecTech.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_blue</item>
        <!--
            Transparent icon: the Android 12+ system splash only supports a
            color + centered icon, so instead of showing the launcher icon we
            show nothing and let the full-bleed launch artwork take over on
            the first frame.
        -->
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.SpecTech</item>
    </style>
</resources>
